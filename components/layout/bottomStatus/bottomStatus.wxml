<view class="bottomStatus" wx:if="{{status}}">
	<image class="loading" wx:if="{{status === 1}}" src='{{cdnImagePath}}gif_listloading@3x.gif'></image>
	<text class="txt" wx:if="{{status === 2}}">没有更多数据</text>
</view>