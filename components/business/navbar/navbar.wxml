<view style="height:{{navH}}px;">
  <view class="navbar" style="height:{{navH}}px;background: {{background}}">
    <view class="return-back" wx:if="{{showBackBtn}}" bindtap="navBack">
    	<i class="icon iconfont icon-zhishibiao" style="color: {{color}}"></i>
    </view>
    <view class="navbar-title" style="color: {{color}}"> {{ title }} </view>
  </view>
</view>
