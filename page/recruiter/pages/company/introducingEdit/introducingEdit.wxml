<view class="company-introdution">
	<navbar title="编辑公司介绍" background="#fff" color="#22292C"></navbar>
	<view class="edit-box">
			<textarea
				bindinput="bindInput"
				placeholder="描述您公司的背景和业务"
				maxlength="10000"
				value="{{intro}}" />
			<view class="controls">
				<view class="sum"><text class="{{intro.length > 5000 ? 'red' : ''}}">{{ intro.length }}</text>/5000</view>
			</view>
		</view>
<!-- 	<view class="head">
		<textarea maxlength="5000" placeholder="描述您公司的背景和业务" value="{{content}}" bindinput="getValue"></textarea>
		<view class="num">{{content.length || 0}}/5000</view>
	</view> -->
	<view class="footer">
    <view class="button-save" bindtap="submit">保存</view>
  </view>
</view>